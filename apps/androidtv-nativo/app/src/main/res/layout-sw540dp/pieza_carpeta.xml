<?xml version="1.0" encoding="utf-8"?>
<!--
  La carpeta de canales, a tres metros del sofá.

  Aquí lo que se lee es el NOMBRE, y con las medidas de antes no cabía:
  entre el cuadro del icono, sus márgenes y una flecha, al texto le
  quedaban ochenta puntos de doscientos cuarenta y «AUTONÓMICOS» salía
  como «AUT…». El icono baja a 38, la flecha desaparece —en una tele no
  aporta: el mando ya dice que se entra con OK— y la columna es más ancha.

  Los mismos identificadores que la del teléfono; la flecha sigue
  declarada porque allí sí sirve, pero aquí nace escondida.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/carpeta"
    android:focusable="true"
    android:clickable="true"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:layout_marginBottom="4dp">

    <FrameLayout
        android:layout_width="38dp"
        android:layout_height="38dp"
        android:duplicateParentState="true"
        android:background="@drawable/hueco_logo">

        <ImageView
            android:id="@+id/icono"
            android:layout_width="21dp"
            android:layout_height="21dp"
            android:layout_gravity="center"
            android:duplicateParentState="true"
            android:tint="@color/icono_carpeta"
            android:contentDescription="@null" />
    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp">

        <TextView
            android:id="@+id/nombre"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/texto"
            android:textSize="19sp"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/cuantos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/tenue"
            android:textSize="13sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginTop="1dp"
            android:visibility="gone" />
    </LinearLayout>

    <ImageView
        android:id="@+id/flecha"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:src="@drawable/ic_alante"
        android:tint="@color/tenue"
        android:visibility="gone"
        android:contentDescription="@null" />
</LinearLayout>
