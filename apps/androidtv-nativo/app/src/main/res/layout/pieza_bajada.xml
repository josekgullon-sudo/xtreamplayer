<?xml version="1.0" encoding="utf-8"?>
<!--
  Una cosa guardada: carátula, nombre, en qué va, y la papelera.

  Verla y quitarla van en la misma fila porque son las dos únicas cosas que
  se hacen aquí; esconder la segunda en un menú de ajustes es lo que hace que
  un disco se llene y no se vacíe nunca. Y si ha fallado, volver a intentarlo,
  que ahí sí hay una tercera.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:layout_marginBottom="8dp">

    <LinearLayout
        android:id="@+id/fila"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/carpeta"
        android:focusable="true"
        android:clickable="true"
        android:padding="12dp">

        <ImageView
            android:id="@+id/cartel"
            android:layout_width="118dp"
            android:layout_height="66dp"
            android:scaleType="centerCrop"
            android:background="@drawable/hueco_logo"
            android:contentDescription="@null" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="14dp">

            <TextView
                android:id="@+id/nombre"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/texto"
                android:textSize="18sp"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/estado"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/tenue"
                android:textSize="14sp"
                android:layout_marginTop="4dp" />

            <!-- Cuánto lleva. Solo mientras baja: con el fichero entero, la
                 barra llena no dice nada que no diga ya el texto -->
            <ProgressBar
                android:id="@+id/barra"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="6dp"
                android:max="100"
                android:progressDrawable="@drawable/barra_bajada"
                android:layout_marginTop="8dp"
                android:visibility="gone" />
        </LinearLayout>
    </LinearLayout>

    <!--
         Volver a intentarlo. Nace escondido y solo sale cuando algo ha
         fallado: la mitad de los fallos son un corte de red de diez
         segundos, y sin este botón la única salida era tirar la descarga a
         la papelera y volver a la ficha a empezar de cero.
    -->
    <ImageView
        android:id="@+id/reintentar"
        android:layout_width="58dp"
        android:layout_height="58dp"
        android:layout_marginStart="12dp"
        android:padding="16dp"
        android:background="@drawable/circulo_accion"
        android:src="@drawable/ic_recargar"
        android:tint="@color/apagado"
        android:visibility="gone"
        android:focusable="true"
        android:clickable="true"
        android:contentDescription="Volver a intentarlo" />

    <ImageView
        android:id="@+id/quitar"
        android:layout_width="58dp"
        android:layout_height="58dp"
        android:layout_marginStart="12dp"
        android:padding="16dp"
        android:background="@drawable/circulo_accion"
        android:src="@drawable/ic_papelera"
        android:tint="@color/apagado"
        android:focusable="true"
        android:clickable="true"
        android:contentDescription="Quitar del aparato" />
</LinearLayout>
