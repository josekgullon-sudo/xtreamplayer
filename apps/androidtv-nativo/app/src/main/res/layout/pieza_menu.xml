<?xml version="1.0" encoding="utf-8"?>
<!--
  Una tarjeta de sección, al modo de .feature-card del web: el icono arriba
  en su cuadrado y el título abajo. Sin alturas fijas: la tarjeta se estira
  con lo que le den y el hueco del medio hace de fuelle.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/tarjeta"
    android:focusable="true"
    android:paddingStart="26dp"
    android:paddingEnd="26dp"
    android:paddingTop="24dp"
    android:paddingBottom="24dp">

    <FrameLayout
        android:layout_width="54dp"
        android:layout_height="54dp"
        android:background="@drawable/insignia">

        <ImageView
            android:id="@+id/icono"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center"
            android:tint="@color/marca_viva"
            android:contentDescription="@null" />
    </FrameLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:minHeight="16dp" />

    <TextView
        android:id="@+id/titulo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/texto"
        android:textSize="23sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/detalle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/apagado"
        android:textSize="15sp"
        android:layout_marginTop="5dp"
        android:maxLines="1"
        android:ellipsize="end" />
</LinearLayout>
