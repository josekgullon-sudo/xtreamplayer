/*
 * Raíz del proyecto: solo dice con qué se compila. Misma versión que la del
 * móvil a propósito — está explicado en apps/android/build.gradle.
 */
plugins {
    id 'com.android.application' version '8.7.3' apply false
}
